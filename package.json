{
  "name": "any-cfg",
  "version": "0.8.0",
  "author": "phaux <phaux4@gmail.com>",
  "description": "Config loader",
  "repository": "phaux/node-any-cfg",
  "license": "ISC",
  "keywords": [
    "config",
    "loader"
  ],
  "main": "lib",
  "typings": "lib",
  "scripts": {
    "docs": "typedoc --out docs --exclude '**/parse/*.ts' --excludeExternals --excludePrivate",
    "prepare": "tsc -p . -m commonjs -t es2017 --outDir lib",
    "prepublishOnly": "npm test",
    "test": "tslint -p . && node test"
  },
  "devDependencies": {
    "@types/node": "^10.12.0",
    "tslint": "^5.11.0",
    "typedoc": "^0.13.0",
    "typescript": "^3.1.3"
  },
  "dependencies": {
    "toml": "^2.3.3"
  }
}
