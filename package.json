{
  "name": "any-cfg",
  "version": "0.7.0",
  "author": "phaux <phaux4@gmail.com>",
  "description": "Config loader",
  "repository": "phaux/node-any-cfg",
  "license": "ISC",
  "keywords": [
    "config",
    "loader"
  ],
  "main": "lib",
  "typings": "lib",
  "scripts": {
    "prepare": "tsc -p . -m commonjs -t es2017 --outDir lib",
    "prepublishOnly": "npm test",
    "test": "tslint -p . && eslint test && node test"
  },
  "devDependencies": {
    "@types/node": "^9.6.6",
    "eslint": "^4.19.1",
    "tslint": "^5.11.0",
    "typescript": "^3.0.1"
  },
  "dependencies": {
    "toml": "^2.3.3"
  }
}
